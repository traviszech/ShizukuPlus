<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:checkable="true"
    android:clickable="true"
    android:focusable="true"
    android:descendantFocusability="afterDescendants"
    app:cardBackgroundColor="?colorSurfaceContainerLow"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    app:cardCornerRadius="@dimen/card_corner_radius">

    <!-- Content frame: inner ViewHolder layouts inflate here -->
    <FrameLayout
        android:id="@+id/card_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/card_content_padding" />

    <!-- Drag handle: shown for draggable cards, semi-transparent grip on right edge -->
    <ImageView
        android:id="@+id/drag_handle"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical|end"
        android:padding="8dp"
        android:src="@drawable/ic_drag_handle"
        android:contentDescription="@null"
        android:alpha="0.30"
        android:visibility="gone" />

    <!-- Remove button: shown in edit mode, top-right corner -->
    <ImageButton
        android:id="@+id/remove_btn"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_gravity="top|end"
        android:layout_margin="6dp"
        android:padding="4dp"
        android:src="@drawable/ic_close_24"
        android:contentDescription="@null"
        android:background="@android:color/transparent"
        android:tint="?attr/colorError"
        android:elevation="2dp"
        android:visibility="gone" />

</com.google.android.material.card.MaterialCardView>
