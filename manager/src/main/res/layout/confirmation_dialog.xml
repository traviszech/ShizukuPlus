<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="?android:dialogPreferredPadding"
    android:orientation="vertical">

    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center"
        android:importantForAccessibility="yes"
        android:contentDescription="@string/accessibility_icon_info"
        android:src="@drawable/ic_system_icon"
        android:tint="?android:colorAccent"
        android:tintMode="src_in" />

    <TextView
        android:id="@android:id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="24dp"
        android:paddingTop="16dp"
        android:textColor="?android:textColorPrimary"
        android:textSize="20sp"
        tools:text="@string/permission_warning_template" />

    <TextView
        android:id="@android:id/button1"
        style="@style/GrantPermissionsButtons.Top"
        android:text="@string/grant_dialog_button_allow_always" />

    <TextView
        android:id="@android:id/button3"
        style="@style/GrantPermissionsButtons.Bottom"
        android:text="@string/grant_dialog_button_deny" />

</LinearLayout>